{{- if and (eq .Values.authMode "apikey") .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  ANTHROPIC_API_KEY: {{ .Values.secret.anthropicApiKey | quote }}
{{- end }}
